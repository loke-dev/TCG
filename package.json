{
  "scripts": {
    "build": "ROLLUP_WASM=true nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "ROLLUP_WASM=true npx nuxi prepare"
  },
  "devDependencies": {
    "@vite-pwa/nuxt": "^0.10.5",
    "nuxt": "^3.13.2",
    "@rollup/wasm-node": "^4.13.0"
  },
  "resolutions": {
    "rollup": "npm:@rollup/wasm-node"
  }
}
