{
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/node",
      "config": {
        "installCommand": "npm install --force"
      }
    }
  ],
  "build": {
    "env": {
      "NPM_FLAGS": "--force"
    }
  }
}
